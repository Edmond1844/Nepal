# Навигация для сайта

## Папка styles

| Файл | Тип | Импорты | Описание | Props | Состояния |
|------|-----------|---------|----------|-------|-----------|
|**theme.css**| `CSS файл`|`-`|`Файл содержит: цвета для тем, размеров и базовую типографику системы.`|`-`|`-`|

## Папка Hero
| Файл | Компонент | Импорты | Описание | Props | Состояния |
|------|-----------|---------|----------|-------|-----------|
|**Hero.jsx**| `Hero` | `useState, useEffect` <br> `languageEng` <br> `languageRus` <br> `ArrowRight` <br> `Link` <br> `Header` <br> `MainButton` | Контейнерный компонент, который объединяет шапку (Header) и основное вводное содержание страницы | `lang` - Текущий язык интерфейса <br> `setLang` - Функция для изменения языка <br> `language` - Объект с переводами для всех языков|

## Папка Header
| Файл | Компонент | Импорты | Описание | Props | Состояния |
|------|-----------|---------|----------|-------|-----------|
|**Header.jsx**| `Header` | `iconBurger` | Компонент шапки сайта с навигацией, логотипом и кнопкой переключения языка. Включает мобильное меню.| `lang` - Текущий язык интерфейса <br> `setLang` - Функция для изменения языка <br> `language` - Объект с переводами для всех языков|

