# Навигация для сайта

## Папка styles

| Файл | Тип | Импорты | Описание | Props | Состояния |
|------|-----------|---------|----------|-------|-----------|
|**theme.css**| `CSS файл`|`-`|`Файл содержит: цвета для тем, размеров и базовую типографику системы.`|`-`|`-`|

## Папка Hero
| Файл | Компонент | Импорты | Описание | Props | Состояния |
|------|-----------|---------|----------|-------|-----------|
|**Hero.jsx**| `Hero` |  `Header` <br> `MainButton` | Контейнерный компонент, который объединяет шапку (Header) и основное вводное содержание страницы | `lang` - Текущий язык интерфейса <br> `setLang` - Функция для изменения языка <br> `language` - Объект с переводами для всех языков|

## Папка Header
| Файл | Компонент | Импорты | Описание | Props | Состояния |
|------|-----------|---------|----------|-------|-----------|
|**Header.jsx**| `Header` | `iconBurger` <br> `Icon`| Компонент шапки сайта с навигацией, логотипом, кнопками переключения языка и переключения цвета темы. Включает мобильное меню.| `lang` - Текущий язык интерфейса <br> `setLang` - Функция для изменения языка <br> `language` - Объект с переводами для всех языков| 
## Папка SectionFastPick
| Файл | Компонент | Импорты | Описание | Props | Состояния |
|------|-----------|---------|----------|-------|-----------|
|**SectionFastPick.jsx**|`SectionFastPick`| `Icon` <br> `FilterButton` <br> `MainButton` | SectionFastPick — Компонент для фильтрации и поиска туристических предложений. Позволяет пользователям находить туры по названию, уровню сложности, продолжительности и ценовому диапазону. Включает в себя список туров| `lang` - Текущий язык интерфейса <br>`language` - Объект с переводами для всех языков|
## Папка SectionGuides
| Файл | Компонент | Импорты | Описание | Props | Состояния |
|------|-----------|---------|----------|-------|-----------|
|**SectionGuides.jsx**|`SectionGuides`| `MainButton` | SectionGuides — Компонент для отображения списка гидов с их фотографиями, информацией и уровнями подготовки. | `lang` - Текущий язык интерфейса <br> `language` - Объект с переводами для всех языков| 

